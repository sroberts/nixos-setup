# Machine-specific — encodes this host's LUKS UUIDs. Stays on disk only.
# Re-staged with `git add -f` after a fresh clone so flake builds can see it,
# but never committed.
hardware-configuration.nix

# nix build output symlinks
result
result-*

# direnv / lorri local state
.direnv/
.envrc.local

# editor scratch
*.swp
*.swo
*~
.DS_Store
